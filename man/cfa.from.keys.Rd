% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cfa.from.keys.R
\name{cfa.from.keys}
\alias{cfa.from.keys}
\title{Runs CFA models for multiple scales based on items in a keys list.}
\usage{
cfa.from.keys(
  keys,
  d,
  name = "cfa",
  out_dir = "output",
  std.lv = TRUE,
  fit_save = TRUE,
  fit_measures = TRUE,
  miss = "ML",
  hash_dir = "hashes"
)
}
\arguments{
\item{keys}{A named list of keys. Names should be scale names, elements should a list of
items included in each scale.}

\item{d}{The data. This must include all observed variables used in any of the models.}

\item{name}{A name for the collection of models. Defaults to 'cfa'.
The name should be unique for each time any function is called from the
package or outputs from other calls will be overwritten.}

\item{out_dir}{The directory where all function outputs will be saved. Defaults to 'output'.}

\item{std.lv}{Sets the `std.lv` param, as per lavaan. Defaults to `TRUE`.}

\item{fit_save}{`TRUE` to save model fit measures. `FALSE` otherwise.}

\item{fit_measures}{A vector of fit measures to save, or `NULL` to select all fit measures.
Defaults to `NULL`. Irrelevant if `fit_save = FALSE`.}

\item{miss}{Sets the `missing` param, as per lavaan. Defaults to 'ML'.}

\item{hash_dir}{A subdirectory of `out_dir` where data hashes are saved.
Defaults to 'hashes'.}
}
\value{
Returns a list of lists.
The elements are a list of lavaan CFA model output objects;
a list of parameter estimates from the models (standardized if `std = TRUE`);
and, if `fit_measures` is not FALSE, a matrix of fit measures for each model.
}
\description{
cfa.from.keys runs a confirmatory factor analysis (CFA) model for each
element of a keys list. The keys list must be a named list of scales, where
each element is an item from the corresponding scale. The function is
designed to streamline running CFA models for all scales in a sample and to
input model outputs into downstream functions.
}
