% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/efa.from.keys.R
\name{efa.from.keys}
\alias{efa.from.keys}
\title{Runs an EFA model based on items in a keys list.}
\usage{
efa.from.keys(
  keys,
  data,
  name = "efa",
  out_dir = "output",
  orthogonal = FALSE,
  std.lv = TRUE,
  fit_save = TRUE,
  fit_measures = "all",
  miss = "ML",
  est = "default",
  check = TRUE,
  save_out = FALSE
)
}
\arguments{
\item{keys}{A named list of keys. Names must be factor names, elements must be
vectors of items that should be targeted to load on the factor.}

\item{data}{The data. This must include all observed variables in any of the keys.}

\item{name}{A subdirectory where model outputs will be saved when \code{save_out = TRUE}.
Defaults to 'efa'. Irrelevant if both \code{save_out = FALSE} and \code{check = FALSE}.
The name should be unique for each set of models or outputs from other
calls will be overwritten.}

\item{out_dir}{A parent directory for collecting outputs from \code{semFromKeys} function calls
when \code{save_out = TRUE}.
Defaults to 'output'.
Irrelevant if both \code{save_out = FALSE} and \code{check = FALSE}.}

\item{orthogonal}{Sets the \code{orthogonal} parameter, as per lavaan (see \code{\link[lavaan:lavOptions]{lavaan::lavOptions()}}).
Defaults to \code{FALSE}.}

\item{std.lv}{Sets the \code{std.lv} param, as per lavaan (see \code{\link[lavaan:lavOptions]{lavaan::lavOptions()}}).
Defaults to \code{TRUE}.}

\item{fit_save}{\code{TRUE} to include model fit measures in the output. \code{FALSE} otherwise.
\code{FALSE} may be desirable when you do not care about fit measures and where
they may take a long time to estimate.
Fit can still be examined for individual models with something like,
\code{lavaan::fitMeasures(fit$fit$model)}.}

\item{fit_measures}{A vector of fit measures to save or 'all' to select all fit measures,
as per the \code{fit.measures} parameter from lavaan's \code{\link[lavaan:fitMeasures]{lavaan::fitMeasures()}}
function.
Defaults to 'all'. Irrelevant if \code{fit_save = FALSE}.}

\item{miss}{Sets the \code{missing} parameter, as per lavaan (see \code{\link[lavaan:lavOptions]{lavaan::lavOptions()}}).
Defaults to 'ML'.}

\item{est}{Sets the \code{estimator} parameter, as per lavaan (see \code{\link[lavaan:lavOptions]{lavaan::lavOptions()}}).
The default ('default') uses the lavaan default for the model being run.}

\item{check}{Should the code check to see if previous outputs have been saved?
If \code{TRUE}, the model will not run if model code, parameters, and a data hash
exist and have not changed and output is of the correct class.
If \code{FALSE}, the model will run regardless of the existence of previous
outputs.}

\item{save_out}{Should outputs be saved to enable checking next time?
If \code{TRUE}, model code, a hash of the data, important input parameter values,
and output will be saved.
If \code{check = TRUE} the next time the code is run,
then saved inputs will be checked against current values to determine whether
the code needs to be rerun.
If not (and the outputs are the correct class),
then the outputs from the previous run will be returned.
If \code{FALSE}, nothing will be saved, the output will simply be returned as per
normal R functioning. Next time the code is run, models will be re-estimated
regardless of changes to code or data.}
}
\value{
Returns a list of lists.
The elements are a list of lavaan bifactor model output objects;
a list of parameter estimates from the models (standardized if \code{std = TRUE});
and, if \code{fit_save = TRUE}, a matrix of fit measures for each model.
}
\description{
efa.from.keys runs a exploratory factor analysis (EFA) in lavaan with a
rotation targeted based on a keys list.
}
\details{
The function was designed to streamline running exploratory structural
equation models (ESEM) using Burt's (1976) 2-stage procedure to prevent
interpretational confounding in the context of ESEM. However, it can also be
used to easily run a targeted EFA with only a keys list to avoid having to
manually specify the target and model.

The function was designed for use with established multidimensional scales,
such that a target is always reasonable.
The function does not currently support untargeted rotations.

The model relies on \code{\link[=sem.check]{sem.check()}} for the back-end of running the models.
This enables saving inputs and outputs from model runs
(with \code{save_out = TRUE}) and checking to see if anything has changed from
prior runs before running again (with \code{check = TRUE}).
The functionality was included for a number of very slow models or a lot of
faster models, such that time spent rerunning them would be onerous.
For further details on how this works, see the \code{\link[=sem.check]{sem.check()}} function
documentation.
}
\examples{
# Create EFA keys
# Using only 3 factors to save time
keys_e0 <- paste0("bfi_", c("e", "a", "c"))
# Using less than all items to save time
# (This results in a less than ideal solution but it shouldn't matter for an
# example)
keys_e <- sapply(
  keys_e0,
  function(x) {
    names(BFIGritHope)[grep(paste0(x, "\\\\d_[1-2]"), names(BFIGritHope))]
  },
  simplify = FALSE
)
# Run model
efa_fit <- efa.from.keys(keys_e, BFIGritHope, check = FALSE, fit_save = TRUE)
# Examine results
summary(efa_fit$fit$efa)  # Standard lavaan summary
efa_fit$par$efa           # Parameter estimates
efa_fit$fit_measures      # Fit measures
}
\references{
Burt, R. S. (1976).
Interpretational confounding of unobserved variables in Structural Equation
Models. Sociological Methods & Research, 5(1), 3-52.
doi.org/10.1177/004912417600500101.
}
\seealso{
\code{\link[=sem.check]{sem.check()}}, which \code{efa.from.keys()} uses for all the back-end, and
\code{\link[lavaan:sem]{lavaan::sem()}}, which is used to estimate the models.
}
