% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bifactor.from.keys.R
\name{bifactor.from.keys}
\alias{bifactor.from.keys}
\title{Runs bifactor models for multiple scales based on items in a keys list.}
\usage{
bifactor.from.keys(
  keys_g,
  keys_b,
  keys,
  d,
  name = "bifactor",
  out_dir = "output",
  std.lv = TRUE,
  fit_save = TRUE,
  fit_measures = TRUE,
  miss = "ML",
  hash_dir = "hashes",
  mod_dir = NULL
)
}
\arguments{
\item{keys_g}{A named list of general factors. Names must be the names of the general
factors, elements are vectors of items that will load on the general factor.}

\item{keys_b}{A named list of group factors. Names must be the general factors.
Elements must be the group factor names.}

\item{keys}{A named list of items of group factors. Names must be group factor names.
Elements must be items that load on the group factors.
Elements must include group factors across all bifactor models.}

\item{d}{The data. This must include all observed variables used in any of the models.}

\item{name}{A name for the collection of models. Defaults to "bifactor".}

\item{out_dir}{The directory where all function outputs will be saved. Defaults to 'output'.}

\item{std.lv}{Sets the `std.lv` param, as per lavaan. Defaults to `TRUE`.}

\item{fit_save}{`TRUE` to save model fit measures. `FALSE` otherwise.}

\item{fit_measures}{A vector of fit measures to save, or `NULL` to select all fit measures.
Defaults to `NULL`. Irrelevant if `fit_save = FALSE`.}

\item{miss}{Sets the `missing` param, as per lavaan. Defaults to 'ML'.}

\item{hash_dir}{A subdirectory of `out_dir` where data hashes are saved.
Defaults to 'hashes'.}

\item{mod_dir}{A subdirectory of `out_dir` where model outputs are saved.
If NULL, it will be set as 'name'.}
}
\value{
Returns a list of lists.
The elements are a list of lavaan bifactor model output objects;
a list of parameter estimates from the models (standardized if `std = TRUE`);
and, if `fit_measures` is not FALSE, a matrix of fit measures for each model.
}
\description{
cfa.from.keys runs a confirmatory factor analysis (CFA) model for each
element of a keys list. The keys list must be a named list of scales, where
each element is an item from the corresponding scale. The function is
designed to steamline running CFA models for all scales in a sample and to
input model outputs into downstream functions.
}
